function Loading() {
  return (
    <div className="fixed inset-0 flex items-center justify-center w-full h-screen bg-background ">
      <span className="block w-20 h-20 border-[10px] rounded-full border-secondary border-r-primary animate-spin" />
    </div>
  );
}

export default Loading;
